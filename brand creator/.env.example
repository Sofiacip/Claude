# Impact OS — Brand Module
# Copy this file to .env and fill in your values

# No API key needed — uses Claude Max subscription via `claude login`
# Make sure you've run `claude login` and authenticated with your Max account

# Password to access the tool (shared with the team)
APP_PASSWORD=

# Port for the local server (default: 3000)
PORT=3000
